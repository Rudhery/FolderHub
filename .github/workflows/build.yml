name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore app/FolderHub.sln

      - name: Build
        run: dotnet build app/FolderHub.sln --no-restore -c Release

      - name: Test
        run: dotnet test app/FolderHub.sln --no-build -c Release

      - name: Publish
        run: >
          dotnet publish app/src/FolderHub/FolderHub.csproj
          -c Release -r win-x64 -o dist
          -p:SelfContained=false
          -p:PublishSingleFile=true
          -p:DebugType=none

      - uses: actions/upload-artifact@v7
        with:
          name: FolderHub
          path: dist/FolderHub.exe
